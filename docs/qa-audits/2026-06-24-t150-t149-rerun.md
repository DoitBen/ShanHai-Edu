# T150 T149 后窄范围复测

日期：2026-06-24
角色：测试工程师
范围：只复测 T149 修复的 T148 失败面，不做真实 provider、生产权限、多浏览器或全量重型测试
证据目录：`docs\qa-audits\t150-t149-rerun-evidence\20260624-170324`

## 结论

【通过】。

T149 三个浏览器失败面均已通过真实浏览器复测：最终交付普通区不再暴露内部路径、工程文件名、JSON 片段或 `manifest` / `StateEngine`；点击 `PPT 草稿 可回看` 和 `视频生成 可回看` 后主任务区均切换到对应内容，不再停留在最终交付；浏览器 console `error/warn` 为空，未再出现 React duplicate key `摘要 17`。

T148 同口径四类下载代理补测通过：Next 代理下载 PPTX、MP4、PDF、Markdown 均返回 200。基于 T150 证据，T148 浏览器阻塞面可关闭。

本轮额外发现一个非 T149 范围关注项：`final_delivery` 生成的嵌套 PPTX 路径 `exports/final_delivery/...pptx` 文件实际存在，但当前下载路由不支持 `/exports/final_delivery/...` 这种嵌套路径，API/Next 代理请求返回 404。T148 原下载口径使用 `pptx_artifact` 的 `/exports/<filename>.pptx`，该路径本轮返回 200，因此不阻塞 T149/T150 关闭，但建议后端或最终交付下载入口后续单独收口。

## 测试环境

- API：`http://127.0.0.1:8150`
- Web：`http://127.0.0.1:3150`
- Storage：`storage-t150-t149-rerun`
- Provider：fake / placeholder
- 测试用户：`qa-t150`
- 项目：`proj_3e68b8633bb9`
- 项目名：`T150-T149窄范围复测`

本轮未读取、未打印真实密钥，未打开 `.env`，未修改业务代码。

## 复测结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| 真实浏览器登录 `qa-t150` | 通过 | `browser-after-login-state.json`、`browser-dashboard-snapshot.txt` |
| 新项目推进到 `final_video=approved`，浏览器生成 `final_delivery=needs_review` | 通过 | `api-prep-summary.json`、`api-get-final_delivery-after-browser.json` |
| 点击 `PPT 草稿 可回看` 后主任务区显示 PPT 内容而非最终交付 | 通过 | `browser-workspace-ppt-draft-state.json`，`hasFinalDeliveryAsTask=false`，包含 PPT 子门禁 |
| 点击 `视频生成 可回看` 后主任务区显示视频生成内容而非最终交付 | 通过 | `browser-workspace-video-generation-state.json`，`hasFinalDeliveryAsTask=false`，包含文稿、剧本、资产、分镜、合成内容 |
| 最终交付普通区红线扫描 | 通过 | `browser-workspace-target-final-dom-redline-scan.json`：`redlineHits=[]`、`pathLeakHits=[]`、`bodyVisibleHits=[]` |
| 普通最终交付区无 evidence / JSON 结构化预览入口 | 通过 | `browser-workspace-final-delivery-after-generate-state.json`：`hasForbiddenEvidencePreviewButtons=false`、`forbiddenButtons=[]` |
| 控制台 error/warn 为空，未出现 `摘要 17` | 通过 | `browser-console-target-final.json=[]`、`browser-console-after-final-delivery-generate.json=[]` |
| Next 代理下载 PPTX、MP4、PDF、Markdown | 通过 | `download-checks-summary.json`：四类资源均 200 |

## 关键证据摘要

- 最终交付普通区只展示教师可读摘要：`教案材料`、`PPT 文件`、`导入视频`、`材料检查`、`交付状态`、`试讲提醒`。
- 普通区扫描未命中：`exports/final_delivery`、`delivery_manifest.json`、`gate_result.json`、`JSON`、`manifest`、`StateEngine`。
- 普通区按钮仅包含常规流程按钮和 `确认并进入下一步`，未出现 evidence 预览、JSON 结构化预览或内部文件入口。
- PPT 回看主任务区显示 `PPT 草稿 / PPT 装配`，并包含结构方案、逐页脚本、视觉资产、PPTX 文件检查点。
- 视频回看主任务区显示 `视频生成 / 视频生成+旁白+拼接`，并包含文稿、分场剧本、资产与首帧、分镜、clip/TTS/合成内容。

## 准备过程说明

API 准备阶段第一次在 `ppt_page_script/generate` 触发 `409 / UPSTREAM_NOT_APPROVED`，原因是测试准备脚本最初把 `visual_contract` / `character_dict` 作为嵌套对象放进创建项目 payload，而当前创建项目模型不接收这两个嵌套字段。随后已通过公开节点 generate/approve 接口补齐依赖，未绕过状态机。证据：`api-generate-ppt_page_script.error.json`、`api-prep-summary.json`。

## 下载关注项

通过项按 T148 原口径检查：

- PPTX：`/api/backend/projects/proj_3e68b8633bb9/exports/T150-T149窄范围复测-pptx-artifact.pptx`，200
- MP4：`/api/backend/projects/proj_3e68b8633bb9/outputs/final_video.mp4`，200
- PDF：`/api/backend/projects/proj_3e68b8633bb9/files/knowledge-points/kp_001/source.pdf`，200
- Markdown：`/api/backend/projects/proj_3e68b8633bb9/files/knowledge-points/kp_001/mineru.md`，200

关注项：`/api/backend/projects/proj_3e68b8633bb9/exports/final_delivery/T150-T149窄范围复测-pptx-artifact.pptx` 返回 404，尽管 storage 中该文件存在。证据：`api-download-checks.json`、`next-proxy-download-checks.json` 与 `download-checks-summary.json`。

## 清理

本轮明确启动的 API/Web 进程已清理。`server-cleanup.json` 显示 `api_port_free=true`、`web_port_free=true`；复查未发现 T150 相关残留进程，`.next\dev\lock` 不存在。

## 剩余风险

- 本轮只覆盖 fake/placeholder + 本地真实浏览器 + T149 失败面，不代表真实文本、图片、视频或 TTS provider 通过。
- 不代表生产 RBAC/JWT/session、多浏览器、移动端、容器冷启动或真实 PPT/视频质量通过。
- `final_delivery` 嵌套导出路径 404 需要后续单独决定是否纳入最终交付下载入口修复。
